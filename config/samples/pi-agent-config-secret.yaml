apiVersion: v1
kind: Secret
metadata:
  name: pi-agent-config
  namespace: default
type: Opaque
stringData:
  settings.json: |
    {
      "provider": "freetoken",
      "model": "gtp-oss-20b"
    }
  models.json: |
    {
      "providers": {
        "freetoken": {
          "baseUrl": "http://172.17.0.1:1919/v1",
          "api": "openai-completions",
          "apiKey": "lm-studio",
          "models": [
            {
              "id": "gtp-oss-20b",
              "input": ["text"]
            }
          ]
        }
      }
    }
  models-store.json: |
    {}
  auth.json: |
    {}
