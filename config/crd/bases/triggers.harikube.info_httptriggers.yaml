---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: httptriggers.triggers.harikube.info
spec:
  group: triggers.harikube.info
  names:
    kind: HTTPTrigger
    listKind: HTTPTriggerList
    plural: httptriggers
    singular: httptrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.watch
      name: Watch
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: HTTPTrigger is the Schema for the HTTPtriggers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HTTPTriggerSpec defines the desired state of HTTPTrigger.
            properties:
              concurrency:
                default: 1
                description: Concurency represent the number of parallel triggers.
                maximum: 100
                minimum: 0
                type: integer
              endpoint:
                description: Endpoint represents different endpoint generators.
                properties:
                  auth:
                    description: Auth represents different authentication methods.
                    properties:
                      basicAuth:
                        description: BasicAuth represents the basic authentication
                          details.
                        properties:
                          secretKeyRef:
                            description: SecretKeyRef represents the password secret
                              reference.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          user:
                            description: User represents the name of the user.
                            type: string
                        required:
                        - secretKeyRef
                        - user
                        type: object
                      tls:
                        description: TLS represents TLS authentication details.
                        properties:
                          caRef:
                            description: CARef represents the CA secret reference.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          keyRef:
                            description: KeyRef represents the key secret reference.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - caRef
                        - keyRef
                        type: object
                    type: object
                  body:
                    description: Body represents the body of the request.
                    properties:
                      contentType:
                        description: ContentType represents the content type of the
                          body.
                        type: string
                      template:
                        description: Template represents the template to generate
                          body.
                        type: string
                    type: object
                  delivery:
                    description: Delivery represents the delivery details.
                    properties:
                      compactInterval:
                        description: Timeout represents the timeout of the request.
                        format: duration
                        type: string
                      retries:
                        description: Retries represents the retries of the request
                          on failure.
                        maximum: 100
                        minimum: 0
                        type: integer
                      signature:
                        description: Signature represents message signature generator.
                        properties:
                          hmac:
                            description: HMAC represents HMAC message signature generator.
                            properties:
                              algorythm:
                                default: SHA256
                                description: Algorythm represents the has algorythm
                                  type.
                                enum:
                                - SHA256
                                type: string
                              hmac:
                                description: Header represents the header key of the
                                  signature.
                                type: string
                              keySecretRef:
                                description: KeySecretRef represents signature key
                                  secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            required:
                            - hmac
                            - keySecretRef
                            type: object
                        type: object
                    type: object
                  headers:
                    description: Headers represends extra headers of the request.
                    properties:
                      fromSecretRef:
                        description: FromSecret represents header from secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      static:
                        additionalProperties:
                          type: string
                        description: Static represents fixed header.
                        type: object
                      template:
                        additionalProperties:
                          type: string
                        description: 'Template represents dynamic header. Example:
                          X-Resource-Name: "{{ .metadata.name }}"'
                        type: object
                    type: object
                  service:
                    description: Service represents a service based endpoint.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      static:
                        description: Namespace represents the namespace of service.
                        type: string
                      uriTemplate:
                        description: 'URITemplate represents the URI template of the
                          endpoint. Example: /function/foo/{{ .metadata.name }}.'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  static:
                    description: Static represents a fixed endpoint.
                    type: string
                  uriTemplate:
                    description: 'URLTemplate represents the URL template of the endpoint.
                      Example: https://gateway.example.com/function/foo/{{ .metadata.name
                      }}.'
                    type: string
                type: object
              eventFilter:
                description: 'EventFilter represents a filter expression. Example:
                  old.status.availableReplicas != new.status.availableReplicas.'
                type: string
              eventTypes:
                default:
                - ADDED
                - MODIFIED
                - DELETED
                description: EventType represents the type of events to trigger.
                items:
                  enum:
                  - ADDED
                  - MODIFIED
                  - DELETED
                  type: string
                type: array
              fieldSelectors:
                description: FieldSelector represents list of field selectors.
                items:
                  type: string
                type: array
              labelSelectors:
                description: LabelSelector represents list of label selectors.
                items:
                  type: string
                type: array
              meta:
                description: Meta represents the object kind.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                type: object
              namespaces:
                description: Namespaces represents the namespaces to watch.
                items:
                  type: string
                type: array
              sendInitialEvents:
                default: false
                description: SendInitialEvents represents initial object state triggers.
                type: boolean
            required:
            - endpoint
            - meta
            type: object
          status:
            description: HTTPTriggerStatus defines the observed state of HTTPTrigger.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
