apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: secondary-pi-trigger-job
spec:
  timeouts:
    exec: 4m
  skipDelete: true
  namespace: pi-trigger-secondary
  steps:
  - name: Prepare namespace
    try:
    - script:
        content: |
          kubectl delete namespace pi-trigger-secondary --ignore-not-found=true --wait=true || true
          kubectl create namespace pi-trigger-secondary --dry-run=client -o yaml | kubectl apply -f -
  - name: Create PiTrigger
    try:
    - script:
        content: |
          set -euo pipefail
          IMAGE="ghcr.io/harikube/pi-runner:latest"

          cat <<EOF | kubectl apply -f -
          apiVersion: v1
          kind: Secret
          metadata:
            name: pi-agent-config
            namespace: pi-trigger-secondary
          stringData:
            settings.json: '{"provider":"test"}'
            models.json: '[]'
            models-store.json: '{}'
            auth.json: '{}'
          ---
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: pi-agent-prompts
            namespace: pi-trigger-secondary
          data:
            review.md: |
              Review prompt.
          ---
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: pi-agent-skills
            namespace: pi-trigger-secondary
          data:
            SKILL.md: |
              # skill
          ---
          apiVersion: triggers.harikube.info/v1
          kind: PiTrigger
          metadata:
            name: pi-trigger-secondary
            namespace: pi-trigger-secondary
          spec:
            resource:
              apiVersion: v1
              kind: ConfigMap
            namespaces:
            - pi-trigger-secondary
            labelSelectors:
            - watch-e2e-pi-secondary=true
            eventTypes:
            - ADDED
            agent:
              image: ${IMAGE}
              configSecretRef:
                name: pi-agent-config
              promptsConfigMapRef:
                name: pi-agent-prompts
              skillsConfigMapRef:
                name: pi-agent-skills
              workingDir: /
              noExtensions: true
              backoffLimit: 0
              ttlSecondsAfterFinished: 60
          EOF
    - assert:
        resource:
          apiVersion: triggers.harikube.info/v1
          kind: PiTrigger
          metadata:
            name: pi-trigger-secondary
            namespace: pi-trigger-secondary
  - name: Create watched ConfigMap
    try:
    - apply:
        resource:
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: pi-trigger-secondary-source
            namespace: pi-trigger-secondary
            labels:
              watch-e2e-pi-secondary: "true"
          data:
            key: value
  - name: Validate second Job execution
    try:
    - script:
        content: |
          set -euo pipefail
          for _ in $(seq 1 60); do
            JOB_NAME="$(kubectl get jobs -n pi-trigger-secondary -l triggers.harikube.info/pitrigger-name=pi-trigger-secondary -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || true)"
            POD_NAME=""
            JOB_ARGS=""
            if [ -n "$JOB_NAME" ]; then
              POD_NAME="$(kubectl get pods -n pi-trigger-secondary -l job-name=${JOB_NAME} -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || true)"
              JOB_ARGS="$(kubectl get job "$JOB_NAME" -n pi-trigger-secondary -o jsonpath='{.spec.template.spec.containers[0].args}' 2>/dev/null || true)"
            fi

            if [ -n "$JOB_NAME" ] \
              && [ -n "$POD_NAME" ] \
              && printf '%s' "$JOB_ARGS" | grep -q -- '--no-session' \
              && printf '%s' "$JOB_ARGS" | grep -q -- '--mode'; then
              kubectl get pitrigger pi-trigger-secondary -n pi-trigger-secondary -o yaml
              kubectl get job "$JOB_NAME" -n pi-trigger-secondary -o yaml
              kubectl get pod "$POD_NAME" -n pi-trigger-secondary -o yaml
              exit 0
            fi

            sleep 2
          done

          echo "expected secondary PiTrigger job execution details were not observed"
          kubectl get pitrigger pi-trigger-secondary -n pi-trigger-secondary -o yaml || true
          kubectl get jobs -n pi-trigger-secondary -o wide || true
          kubectl get pods -n pi-trigger-secondary -o wide || true
          exit 1
